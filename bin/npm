#!/bin/sh

if [ "$1" = "config" ] && [ "$2" = "get" ] && [ "$3" = "registry" ]; then
  echo "https://registry.npmjs.org/"
  exit 0
fi

echo "This project uses Bun. The local npm shim only supports 'npm config get registry'." >&2
exit 1
